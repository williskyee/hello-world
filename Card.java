
public class Card {
	
	/**
	 * creates an string object set representing a bomb, 
	 * that when chosen by the reveal action, 
	 * will set the score/counter to zero and end the game
	 * @return string representing the bomb
	 */
	String generateBomb() {
		
	}
	
	/**
	 * creates an numerical object between 1 and a set number, 
	 * meant to be added to the counter if this oject is greater than 1
	 * @return int representing points
	 */
	int generatePoint() {
		
	}
	
	/**
	 * creates a string that represents the unrevealed hidden object in the grid
	 * displayed to the user 
	 * @return string representing unflipped tiles
	 */
	String generateUnflipped() {
		
	}
}
